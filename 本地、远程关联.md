本地与远程相关联

本地：
git init  
git remote add origin <新建的远程仓库的URL>
git checkout -b main
git pull origin main --allow-unrelated-histories
（解决本地和远程都是最新的，没有任何与本地仓库共享的提交历史）
git add .

git commit -m ‘更新’

git push -u origin main